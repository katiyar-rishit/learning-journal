#include <stdio.h>

int main(){
  int a, b, c , sum;
  printf("Enter a:");
  scanf("%d", &a);
  printf("Enter b:");
  scanf("%d", &b);
  printf("Enter c:");
  scanf("%d", &c);
  sum = a + b + c;
  
  printf("Sum: %d", sum);

  return 0;
}
