#include <iostream>
#include <vector>
#include <algorithm>

#define rpm(k,n) for(int i=k; i<n;i++)

using namespace std;

int main(){
  rpm(0,10){
    cout << "Initiate in process..." << "\n";
  }

  return 0;
}
